# PRD00N — <Nome da fatia> — <épico> — <nome da atividade>

> **Status: 📝 rascunho / 👀 em revisão / ✅ aprovado** — atualize esta linha conforme o documento avança (legenda igual em `SPEC00N.md`/`TASK00N.md`).
> Épico: `<epico-NN-nome>` · Domínio(s) AMBION: `<ex.: integrador, iqar>` · Branch: `<nome-da-branch>` · Fatia N de M do épico.
> Contexto compartilhado da atividade inteira (objetivo geral, status dos épicos) está em `STAGE.md`, na raiz de `docs/<feature>/` — não repita aqui, só referencie.
> Este documento é só negócio/UX. Arquitetura, banco de dados, API e segurança ficam em `SPEC00N.md` (mesma pasta, gerado por `ambion_techspec` a partir deste PRD).

## 1. Objetivo desta fatia
<O que esta fatia especificamente entrega, do ponto de vista de quem usa o produto — um incremento pequeno e demonstrável, não o épico inteiro.>

## 2. Público-alvo / persona afetada
<Quem usa este incremento (perfil de usuário/persona do domínio) e por que ele importa para essa pessoa. "Nenhuma diferença de persona" se for indiferente.>

## 3. PRDs relacionados neste épico
<Outros `PRD00N.md` do mesmo épico que compartilham dependência de negócio com esta fatia (ex.: "PRD002.md pressupõe que o cadastro criado em PRD001.md já exista"). "Nenhum" se esta fatia for autônoma — não pode ser omitido.>

## 4. User stories macro desta fatia
<Formato "Como <persona>, quero <ação>, para <benefício>." Uma ou mais, cobrindo só esta fatia.>

## 5. Escopo desta fatia
### Dentro
<O que esta fatia entrega de fato.>
### Fora
<O que fica para uma fatia posterior, outro épico, ou fora da atividade — inclusive decisões técnicas, que são sempre fora do escopo deste documento.>

## 6. Critério de sucesso desta fatia
<Como saber que esta fatia está pronta do ponto de vista de negócio — comportamento observável pelo usuário final, não detalhe de implementação.>

## 7. Pontos de negócio a validar antes de especificar a técnica
<Decisões/confirmações de negócio ainda em aberto que bloqueiam `ambion_techspec` de desenhar a solução com segurança. "Nenhum" se não houver.>

## Agent skills

### Constituição

Padrões do projeto (frameworks, qualidade de código, testes/TDD, qualidade de manutenção, banco de dados, arquitetura) e premissas inegociáveis. See `docs/agents/constituicao.md` (gerado/revisado pela skill `ambion_constituir`).

### Issue tracker

See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context: root `CONTEXT-MAP.md` points to per-service `CONTEXT.md` files (`aermod`, `api`, `app`, `estatistica`, `integrador`, `iqar`, `migris`, `mqtt-server`). See `docs/agents/domain.md`.
